let Resource;_127‍.x([["Field",()=>Field]]);_127‍.w("./Resource.mjs",[["Resource",["Resource"],function(v){Resource=v}]]);

       class Field {

    constructor(name){
        this.name = name;
        this.owner = null;

        this.resources = [];
        this.generateResources(name);
    }

    generateResources(name){
        if(name === "Forest"){
            let resource = new Resource("Wood", Math.floor(Math.random() * 100), 10);
            this.addResource(resource);
        }
    }

    addResource(resource){
        this.resources.push(resource);
    }

    setOwner(owner){
        this.owner = owner;
    }

}