let Field;_c73‍.x([["GameMap",()=>GameMap]]);_c73‍.w("./Field.mjs",[["Field",["Field"],function(v){Field=v}]]);

       class GameMap {

    constructor(sizeX, sizeY){
        this.map = GameMap.init(sizeX, sizeY);
    }

    static init(x, y){
        let array = [];
        for (let i = 0; i < y; ++i) {
            array[i] = [];
            for (let j = 0; j < x; ++j) {
                array[i][j] = new Field("Forest");
            }
        }
        return array;
    }

    getField(x, y){
        return this.map[y][x];
    }

}

